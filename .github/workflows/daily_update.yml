name: MAJ 2025.xlsx + Elo quotidien

permissions:
  contents: write

on:
  schedule:
    - cron: '00 9 * * *'  # Tous les jours à 11h (heure française)
  workflow_dispatch:       # Permet déclenchement manuel

jobs:
  update:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'

      - name: Install dependencies
        run: pip install pandas requests openpyxl

      - name: Télécharger 2025.xlsx
        run: python update_2025_file.py

      - name: Recalculer elo_probs.csv
        run: python generate_elo_probs.py

      - name: Commit & Push
        run: |
          git config --global user.name "github-actions"
          git config --global user.email "github-actions@github.com"
          git add Données/2025.xlsx elo_probs.csv
          git commit -m "📈 Mise à jour auto : 2025.xlsx + elo_probs.csv" || echo "🟰 Rien à valider"
          git push 
